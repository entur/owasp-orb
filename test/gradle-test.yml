version: 2.1

workflows:
  main:
    jobs:
      - owasp/gradle:
          name: build
          pre-steps:
            - run: echo "cd test; ./gradlew $1" \>> gradlew
          post-steps:
            - run: history

